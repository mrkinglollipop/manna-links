# Why source — Notion + graph + memory

Long-form / decisions category.

## Search

- Notion MCP: feature name, symbol, ticket IDs, PR URLs
- `PYTHONPATH=".python_libs" python3 "/Volumes/Cloud Storage/Graph/query_graph.py" search "<terms>"`
- Memory topics: `grep -ril` under `/Volumes/Cloud Storage/Memory/conversations/topics/`
- Episodes: grep `/Volumes/Cloud Storage/Memory/conversations/episodes.jsonl` and `episodes_<YYYY>.jsonl` archives (see `memory.mdc` retrieval fallback)

## What this category uniquely surfaces

ADRs, specs, meeting notes, captured decisions, topic pages, prior session episodes stating “we picked X because Y.”
