{
  "mcpServers": {
    "notion": {
      "url": "https://mcp.notion.com/mcp"
    },
    "dropbox": {
      "url": "https://mcp.dropbox.com/mcp"
    },
    "openrouter": {
      "url": "https://mcp.openrouter.ai/mcp"
    },
    "notionApi": {
      "command": "npx",
      "args": [
        "-y",
        "@notionhq/notion-mcp-server"
      ],
      "env": {
        "NOTION_TOKEN": "${env:NOTION_TOKEN}"
      }
    },
    "better-design": {
      "url": "https://better-design.com/api/mcp",
      "headers": {
        "Authorization": "Bearer ${env:BETTER_DESIGN_API_KEY}"
      }
    },
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest"
      ]
    },
    "firecrawl": {
      "command": "npx",
      "args": [
        "-y",
        "firecrawl-mcp"
      ],
      "env": {
        "FIRECRAWL_API_KEY": "${env:FIRECRAWL_API_KEY}"
      }
    },
    "khipu-cloud": {
      "url": "https://khipu.45-79-143-191.sslip.io/mcp",
      "headers": {
        "Authorization": "Bearer ${env:KHIPU_GATEWAY_TOKEN}"
      }
    }
  }
}
